<%= content_tag(
  :iframe,
  '',
  :id => "temp_card_frame",
  :class => @user.partner_name? ? "membership_card_frame with_partner" : "membership_card_frame",
  :src => url_for(:action => 'temp_card', :params => {:id => @user.id}),
  :border => '0',
  :hspace => '0',
  :vspace => '0',
  :frameBorder => '0',
  :frameSpacing => '0'
) %>
